// Sempre que o usuário digita algo (ex: via prompt ou formulário),
// o programa deve validar o tipo e o formato dos dados.
let entrada = "abc";

if (isNaN(entrada)) {
  console.log("Erro: valor numérico esperado, mas recebido texto.");
  // Solução: converter ou pedir novamente o valor
}
