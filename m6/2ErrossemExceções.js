// Exemplo: resultado incorreto sem gerar erro
let x = "10";
let y = 5;
let soma = x + y; // resultado é "105", não 15
console.log(soma); // "105" — o código roda, mas o resultado está errado
// Comentário: o erro lógico não dispara exceção — apenas gera um resultado inesperado.
